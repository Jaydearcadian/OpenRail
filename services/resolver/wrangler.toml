name = "openrails-resolver"
main = "src/handler.ts"
compatibility_date = "2024-01-01"

[vars]
# Set via wrangler secret put or dashboard for production
# No secrets needed — aggregator URLs are public
